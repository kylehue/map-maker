import { GlobalThemeOverrides } from "naive-ui";

const themeConfig: GlobalThemeOverrides = {
   common: {
      // primaryColor: "#9263e2",
      // primaryColorHover: "#a57fe7",
      // primaryColorPressed: "#8a5ace",
      // primaryColorSuppl: "#532a94",
      fontFamily: "Inter, Roboto, sans-serif, Arial, Helvetica",
      errorColor: "#f25555",
      errorColorHover: "#ed5f5f",
      errorColorPressed: "#db4848",
      errorColorSuppl: "#a82f2f",
   },
};

export default themeConfig;
